# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env / secrets — NEVER commit API keys (keep *.example templates tracked)
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
.npmrc
.netrc
id_rsa
id_ed25519

# Internal agent/infra notes — local-only, never publish (carries host/deploy details)
CLAUDE.md

# Node / Next.js
node_modules/
frontend/.next/
frontend/out/
npm-debug.log*

# Local model cache (fastembed downloads)
.fastembed_cache/
.cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Eval / runtime artifacts
eval/results/*.json
*.log

# Stray source PDFs downloaded at the repo root (the doc PDF lives under docs/)
/*.pdf
